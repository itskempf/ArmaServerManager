<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="ArmaServerManager.UI.Pages.DashboardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ScrollViewer>
        <StackPanel Margin="24" Spacing="16">
            <TextBlock Text="Dashboard" FontSize="24" FontWeight="Bold"/>
            
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- System Stats -->
                <Border Grid.Column="0" Grid.Row="0" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                        CornerRadius="8" Padding="16" Margin="0,0,8,8">
                    <StackPanel>
                        <TextBlock Text="System Resources" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,12"/>
                        <TextBlock x:Name="CpuUsageText" Text="CPU: 0%" Margin="0,4"/>
                        <TextBlock x:Name="RamUsageText" Text="RAM: 0 MB" Margin="0,4"/>
                        <TextBlock x:Name="UptimeText" Text="Uptime: 0d 0h 0m" Margin="0,4"/>
                    </StackPanel>
                </Border>

                <!-- Active Servers -->
                <Border Grid.Column="1" Grid.Row="0" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                        CornerRadius="8" Padding="16" Margin="8,0,0,8">
                    <StackPanel>
                        <TextBlock Text="Active Servers" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,12"/>
                        <ListView x:Name="ActiveServersList" MaxHeight="200">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding}" Margin="0,4"/>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </StackPanel>
                </Border>

                <!-- Quick Actions -->
                <Border Grid.Column="0" Grid.Row="1" Grid.ColumnSpan="2" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                        CornerRadius="8" Padding="16" Margin="0,8,0,0">
                    <StackPanel>
                        <TextBlock Text="Quick Actions" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,12"/>
                        <StackPanel Orientation="Horizontal" Spacing="12">
                            <Button Content="Install Arma Server" Style="{StaticResource AccentButtonStyle}"/>
                            <Button Content="Download Mods"/>
                            <Button Content="Create Preset"/>
                            <Button Content="Check Updates"/>
                            <Button Content="Update All"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </Grid>
        </StackPanel>
    </ScrollViewer>
</Page>