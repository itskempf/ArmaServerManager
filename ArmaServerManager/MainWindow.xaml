<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="ArmaServerManager.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ArmaServerManager"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="ArmaServerManager v0.9 Beta">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Notification Bar -->
        <InfoBar x:Name="NotificationBar" Grid.Row="0" IsOpen="False" Margin="12,12,12,0" Severity="Informational"/>
        
        <NavigationView x:Name="MainNavigation" Grid.Row="1" IsBackButtonVisible="Collapsed" PaneDisplayMode="Left">
            <NavigationView.MenuItems>
                <NavigationViewItem Icon="Home" Content="Dashboard" Tag="Dashboard"/>
                <NavigationViewItem Icon="Globe" Content="Servers" Tag="Servers"/>
                <NavigationViewItem Icon="Library" Content="Mods" Tag="Mods"/>
                <NavigationViewItem Icon="Bookmarks" Content="Presets" Tag="Presets"/>
                <NavigationViewItem Icon="Setting" Content="Settings" Tag="Settings"/>
            </NavigationView.MenuItems>
            <Frame x:Name="MainFrame" Margin="12"/>
        </NavigationView>
    </Grid>
</Window>
